@model IEnumerable<ManageExpenses.Models.ExpenseListItemModel>

<h1>Lista Twoich wydatków</h1>

<p>
    Ilość wydatków @Model.Count()
</p>


@if (Model.Count() > 0)
{
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th width="25%">Wydatek</th>
                <th width="30%">Kategoria wydatku</th>
                <th width="25%">Wartość</th>
                <th width="20%"></th>
            </tr>
        </thead>
        <tbody>
            @Html.DisplayForModel()

            @*@foreach (var item in Model)
            {
                <tr>
                <td>@item.Name</td>
                <td>@item.CategoryName</td>
                <td>@item.Value</td>
                <td>
                    <a class="btn btn-danger btn-sm" href="/Expenses/Delete/@item.Id">
                        <i class="glyphicon glyphicon-remove"></i> Usuń
                    </a>
                </td>
            </tr>


            }*@
        </tbody>
    </table>
}
else
{
    <div class="panel panel-default">
        <div class="panel-body text-center">
            Brak wydatków
        </div>
    </div>
}


    <div>
        <a href="@Url.Action("Add")" class="btn btn-primary">
            <i class="glyphicon glyphicon-plus"></i> Dodaj nowy wydatek
        </a>
        @*<a href="/Expenses/Add" class="btn btn-primary">
            <i class="glyphicon glyphicon-plus"></i> Dodaj nowy wydatek
        </a>*@
    </div>